How to start the application.

From terminal:
go into directory "bin" and write this commands:

- rmiregistry
- java server.ServerRMI
- java client.logic.Client

the output.txt file is located inside the folder "bin"
