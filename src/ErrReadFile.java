

public class ErrReadFile extends Exception {
	ErrReadFile(int x){ printError(x); }
	
	protected void printError(int x){
		//System.out.println("Exit error 0: il contenuto del file presente nel link non è corretto.");
		// fare qualche altra stampa in base all'errore trovato nel file carpibile con il valore di x
		
		// faccio terminare il programma!
		
	}
}

